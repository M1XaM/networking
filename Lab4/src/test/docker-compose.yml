services:
  test:
    build: .
    environment:
      - LEADER=http://leader:8000
      - FOLLOWERS=http://follower1:8001,http://follower2:8002,http://follower3:8003,http://follower4:8004,http://follower5:8005
    volumes:
      - ./results:/app/results 
    networks:
      - lab4_kvnet

networks:
  lab4_kvnet:
    external: true